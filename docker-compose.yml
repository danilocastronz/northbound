services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: northbound
      POSTGRES_PASSWORD: northbound
      POSTGRES_DB: northbound
    ports:
      - "5432:5432"
    volumes:
      - northbound-data:/var/lib/postgresql/data

volumes:
  northbound-data:
